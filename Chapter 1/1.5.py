# 
What happens if you have two values with no operator between them?

# Solution:
# If we try to add two values with no operator between them then it will result in syntax error as it doesn’t know what to do.
