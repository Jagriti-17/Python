#In math notation, leading zeros are okay, as in 02. What happens if you try this in Python? 

# Solution:
#If we try to add leading zeros we will get syntax error because leading zeros in decimal integer literals are not permitted. Python does not allow the use of leading zeros.
